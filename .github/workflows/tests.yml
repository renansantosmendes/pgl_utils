name: Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.10', '3.11']
        exclude:
          # Reduce matrix to save CI time
          - os: macos-latest
            python-version: '3.9'
          - os: windows-latest
            python-version: '3.9'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          # Install deep_learning module dependencies
          pip install networkx>=2.8.0 matplotlib>=3.5.0 tensorflow>=2.10.0
      
      - name: Lint with flake8
        run: |
          # flake8 pgl_utils/ tests/
          echo "Flake8 linting desabilitado - considere usar ferramentas alternativas como ruff ou pylint"
      
      - name: Format check with black
        run: |
          black --check --target-version py311 pgl_utils/ tests/
      
      - name: Type check with mypy
        run: |
          mypy pgl_utils/ --ignore-missing-imports || true
      
      - name: Run tests
        run: |
          pytest tests/ -v --cov=pgl_utils --cov-report=xml --cov-report=term
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          verbose: true
